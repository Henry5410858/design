# ═══════════════════════════════════════════════════════════════
# REQUIRED - Production Deployment
# ═══════════════════════════════════════════════════════════════

# MongoDB Configuration (REQUIRED)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/designcenter?retryWrites=true&w=majority

# JWT Secret (REQUIRED - use strong random string, not default!)
JWT_SECRET=your-super-secret-jwt-key-here

# Frontend URL for CORS (REQUIRED in production)
# Set this to your Netlify domain, e.g., https://your-site.netlify.app
FRONTEND_URL=https://your-frontend-domain.netlify.app

# Additional CORS origins (optional, comma-separated)
# CORS_ORIGINS=https://alternative-domain.com,https://staging.example.com

# Server Configuration
PORT=4000
NODE_ENV=development

# ═══════════════════════════════════════════════════════════════
# OPTIONAL - Features & Integrations
# ═══════════════════════════════════════════════════════════════

# File Upload Configuration
MAX_FILE_SIZE=50000000
ALLOW_PDF_NON_PREMIUM=true

# Cloudinary Configuration (optional, for image optimization)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# AI Configuration (if using OpenAI)
OPENAI_API_KEY=your-openai-api-key-here

# AWS Configuration (if using S3 for storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket-name

# ═══════════════════════════════════════════════════════════════
# DEPLOYMENT NOTES
# ═══════════════════════════════════════════════════════════════
# 
# When deploying to Render:
# 1. Set FRONTEND_URL to your Netlify domain (with https://)
# 2. Use environment variables for all sensitive data
# 3. Never commit .env files to git
# 4. Rotate JWT_SECRET periodically in production
#
