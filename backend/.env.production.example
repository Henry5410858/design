# Production Environment Variables for Render Deployment
# Copy these to Render Dashboard → Settings → Environment

# ============================================================
# DATABASE
# ============================================================
# MongoDB connection string from MongoDB Atlas
MONGODB_URI=mongodb+srv://[username]:[password]@cluster.mongodb.net/design_center

# ============================================================
# SECURITY
# ============================================================
# JWT secret for token signing (generate a strong random string)
JWT_SECRET=generate_a_strong_random_string_here

# Node environment
NODE_ENV=production

# Server port (Render assigns automatically, but can override)
PORT=4000

# ============================================================
# CORS & FRONTEND
# ============================================================
# Frontend URL (required for CORS)
FRONTEND_URL=https://design-dispute.netlify.app

# Additional CORS origins (comma-separated if multiple)
CORS_ORIGINS=https://design-dispute.netlify.app

# ============================================================
# PDF GENERATION - PUPPETEER & CHROMIUM
# ============================================================
# CRITICAL: These enable PDF generation on Render

# Skip downloading Chromium (use system Chromium instead)
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=false

# Path to system Chromium installation
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser

# Fallback Chrome path
CHROME_PATH=/usr/bin/chromium-browser

# ============================================================
# IMAGE PROCESSING - CLOUDINARY
# ============================================================
# Cloudinary API credentials for image upload
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ============================================================
# OPTIONAL INTEGRATIONS
# ============================================================
# Canva API (if implementing Canva integration)
# CANVA_API_BASE_URL=https://api.canva.com
# CANVA_CLIENT_ID=your_client_id
# CANVA_CLIENT_SECRET=your_client_secret
# CANVA_REDIRECT_URI=https://yourdomain.com/canva/callback

# OpenAI API (for future AI features)
# OPENAI_API_KEY=your_openai_api_key

# ============================================================
# FEATURE FLAGS
# ============================================================
# Allow non-premium users to generate PDFs (dev only - remove in production)
# ALLOW_PDF_NON_PREMIUM=true

# ============================================================
# DATABASE RETRY CONFIGURATION (Advanced)
# ============================================================
# Milliseconds between MongoDB connection retries
MONGODB_RETRY_DELAY_MS=2000

# Maximum delay between retries
MONGODB_MAX_RETRY_DELAY_MS=30000

# ============================================================
# IMPORTANT SECURITY NOTES
# ============================================================
# 1. NEVER commit this file to git
# 2. Use strong, unique JWT_SECRET (min 32 characters)
# 3. Rotate JWT_SECRET periodically
# 4. Use environment variables, never hardcode secrets
# 5. Review CORS_ORIGINS regularly (only trusted domains)
# 6. Keep Cloudinary credentials private
# 7. Monitor API usage and costs