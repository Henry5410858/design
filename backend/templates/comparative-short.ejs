<!DOCTYPE html>
<html lang="<%= locale || 'es' %>">
<head>
  <meta charset="utf-8" />
  <title>Comparativa Corta</title>
  <style>
    :root { --primary: <%= theme?.primary || '#1f2937' %>; --secondary: <%= theme?.secondary || '#6366f1' %>; --text: #111827; --muted: #6b7280; }
    body { margin: 0; font-family: 'Segoe UI', Arial, sans-serif; color: var(--text); max-height: 100vh; overflow: hidden; }
    .cover { height: 200px; background: var(--primary); color: #fff; display: flex; align-items: center; padding: 28px 40px; position: relative; }
    .lupaprop-logo { position: absolute; top: 20px; right: 40px; }
    .logo { max-height: 56px; max-width: 180px; }
    .container { padding: 24px 40px; }
    .grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
    .card { border: 1px solid #e5e7eb; border-radius: 8px; overflow: hidden; }
    .card img { width: 100%; height: 160px; object-fit: cover; display: block; }
    .card-body { padding: 12px 14px; }
    .name { font-weight: 700; margin: 0 0 4px; }
    .meta { font-size: 12px; color: var(--muted); }
    .desc { margin-top: 8px; font-size: 13px; }
    .row { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-top: 14px; }
    .badge { padding: 2px 8px; font-size: 12px; border-radius: 999px; background: #eef2ff; color: #3730a3; display: inline-block; }
  </style>
</head>
<body>
  <header class="cover">
    <% if (theme?.logoUrl) { %>
      <img class="logo" src="<%= theme.logoUrl %>" alt="Logo" />
    <% } %>
    <h1 style="margin-left:16px">Comparativa de Propiedades</h1>
    <!-- LupaProp logo in top right -->
    <div class="lupaprop-logo">
      <img src="/assets/lupaprop-logo.png" alt="LupaProp Logo" style="max-height: 50px; max-width: 150px; object-fit: contain;" />
    </div>
  </header>
  <main class="container">
    <section class="grid">
      <% (items || []).slice(0,3).forEach(function(it){ %>
        <article class="card">
          <% if (it._thumb) { %>
            <img src="<%= it._thumb %>" alt="<%= it.title || '' %>">
          <% } %>
          <div class="card-body">
            <h3 class="name"><%= it.title || '' %></h3>
            <div class="meta"><%= [it.location, currency(it.price, currencyCode)].filter(Boolean).join(' • ') %></div>
            <div class="row">
              <div><span class="badge">Beneficios</span><br><%= (it.benefits || []).slice(0,3).join(', ') %></div>
              <div><span class="badge">Desempeño</span><br><%= (it.performance || []).slice(0,3).join(', ') %></div>
            </div>
            <div class="desc"><%= it.description || '' %></div>
          </div>
        </article>
      <% }) %>
    </section>
  </main>
</body>
</html>